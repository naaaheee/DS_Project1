#include "Manager.h"

int main()
{
    Manager manager; // Create an instance of the Manager class
    manager.Run("command.txt"); // Run the Manager with the specified command file

    return 0; // Return 0 to indicate successful completion
}
